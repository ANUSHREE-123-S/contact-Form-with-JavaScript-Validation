# contact-Form-with-JavaScript-Validation
This project is a simple and responsive **Contact Form** that includes **client-side validation** using JavaScript.  
It validates the user's name, email address, and message before allowing form submission.

---

## 🚀 Features

- ✔ HTML contact form (Name, Email, Message)
- ✔ JavaScript validation for:
  - Empty fields
  - Valid email format (Regex)
- ✔ Error messages shown below each input
- ✔ Prevents submission if validation fails
- ✔ Success message on valid submission
- ✔ Clean and responsive UI using CSS
- ✔ No backend required — client-side only
